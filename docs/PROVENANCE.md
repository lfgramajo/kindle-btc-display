# Release Provenance

The v1.7.0 public-release line is staged from the v1.6.3-r3 functional core after extended daily-use field testing. Release preparation deliberately separates the exact private deployment freeze from the public source tree so local runtime configuration, logs, caches, addresses, and host-specific artifacts are never required for publication.

Public-release packaging/documentation changes are validated independently before the final Git tag.
